[default.extend-words]
# Preserved verbatim from GraphRAG 3.1.0's DRIFT primer prompt.
communites = "communites"

[files]
extend-exclude = ["CHANGELOG.md", "vendors/"]

[default]
extend-ignore-re = [
    # 忽略 64 位十六进制哈希及其版本后缀，例如 <hash>_v4
    "\\b[0-9a-fA-F]{64}(?:_v[0-9]+)?\\b",
]
