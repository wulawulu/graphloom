# GraphRAG LLM Cache fixture

此目录保存 GraphRAG `JsonCache` 写出的真实 cache payload：

- `completion.json`：`extract_graph` completion 响应；
- `embedding.json`：包含五个向量的 embedding 响应。

来源：`../graphrag/ragdebug/cache`，GraphRAG commit
`79ab7c9ad586856e82635264c200d8a1eb3c63d9`。

Fixture 不包含 API key 或请求 header。
